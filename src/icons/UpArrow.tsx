import ArrowOutwardIcon from "@mui/icons-material/ArrowOutward";

const iconStyle={
transform:"scale(0.8)"
}

const UpArrow = () => {
  return (
   <ArrowOutwardIcon fontSize='small' sx={iconStyle}/>
  )
}

export default UpArrow
